// Dependencies
const router = require("express").Router();
const db = require("../../models");

// // Request without an id
// router.route("/users")
//     .get((req, res) => {

//     })
//     .post();